app:
  name: Todo
  description: Todo back in spring

spring:
  datasource:
    url: jdbc:h2:file:./mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: create-drop
  h2:
    console.enabled: true